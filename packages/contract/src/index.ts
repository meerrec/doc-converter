/**
 * Общий контракт API сервиса конвертации документов в PDF.
 *
 * Единственный источник правды по формам запросов, ответов, форматов
 * и кодов ошибок. Пакет потребляют обе стороны: сервер (`src/`) валидирует
 * входящие запросы теми же схемами, веб-интерфейс (`web/`) берёт отсюда типы
 * и разбирает ответы.
 *
 * Схемы написаны на zod: из одной схемы выводится и тип, и рантайм-проверка,
 * поэтому они не могут разойтись между сервером и клиентом.
 *
 * Это точка входа для сервера. Веб-интерфейсу валидатор не нужен, поэтому
 * ему доступны подпути (`@doc-converter/contract/formats` и соседние) —
 * они ведут в модули без zod, и импорт константы не тянет валидатор
 * в клиентский бандл. Подробнее — в комментарии к `schemas.ts`.
 */

export * from './formats.js';
export * from './errors.js';
export * from './jobs.js';
export * from './conversion.js';
export * from './health.js';
export * from './schemas.js';
