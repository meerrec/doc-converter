/**
 * Наблюдаемость: логирование и аудит.
 *
 * Отдельный пакет, а не модуль внутри приложения: аудит-лог пишет и API
 * (отказы при приёме файла), и воркер (исход конвертации), причём воркер
 * не поднимает Nest вовсе. Пока эти функции лежали в `nest/common`, воркер
 * импортировал их из чужого приложения — граница между доменом и HTTP-слоем
 * проходила по каталогу, а не по зависимости.
 */

export * from './logger.js';
export * from './audit-log.js';
