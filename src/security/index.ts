/**
 * Модули безопасности сервиса.
 *
 * Осталось два: лимиты (zip-контейнер и сигнатуры) и проверка сигнатур
 * файлов. XML-гард и SSRF-гард удалены вместе с путями, которые они
 * защищали: XML-бомбы приходили через OOXML-документы других форматов,
 * а загрузка по URL из API убрана — файл приходит только в теле запроса.
 *
 * Комментарии на русском языке.
 */

export * from './limits.js';
export * from './magicBytes.js';
export * from './zipGuard.js';
